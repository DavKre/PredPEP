# --- Heavy, out-of-band assets (provided alongside the repo) ---
# Extracted tool blobs (~23 GB)
blobs/*.tar.gz
# Local test inputs copied out of the running container
testdata/

# --- Claude Code / AI agent local config (not part of the app) ---
.claude/
CLAUDE.local.md
.aider*
.cursor/

# --- Build / runtime artifacts ---
*.log

# --- Python ---
__pycache__/
*.py[cod]

# --- Superseded / reference cruft (kept on disk, out of the clean repo) ---
usr_local_bin/
old_scripts/
**/*_bak*
**/*.py3

# Image export from `docker save` (see README "Distributing the image")
predpep-*.tgz
*.tgz
