# Equivalent to run.sh: predpep_app, detached, GPU, restart=unless-stopped.
# For code-iteration bind-mounts, use run-dev.sh instead.

services:
  predpep:
    image: predpep:local
    container_name: predpep_app
    ports:
      - "6363:6363"
    volumes:
      - predpep_data:/tmp/pepspec
    restart: unless-stopped

volumes:
  predpep_data:
